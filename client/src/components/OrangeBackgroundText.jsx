const OrangeBackgroundText = ({ text }) => {
  return (
    <div className="bg-orange-500 text-white py-2 px-4 text-left rounded-sm">
      {text}
    </div>
  );
};

export default OrangeBackgroundText;
